import { FirebaseImageDirective } from './firebase-image.directive';

describe('FirebaseImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FirebaseImageDirective();
    expect(directive).toBeTruthy();
  });
});
